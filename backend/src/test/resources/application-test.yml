spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:cost-count-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      encoding: UTF-8
  h2:
    console:
      enabled: false
